# Example environment configuration for goduit

# Server Configuration
SERVER_ADDRESS=:3000

# Database Configuration
DB_HOST=mongodb
DB_PORT=27017
DB_USER=goduit
DB_PASS=goduit-password
DB_URL=mongodb://goduit:goduit-password@mongo:27017/

# Queue Configuration
# Supported types: rabbitmq, redis
#
# RabbitMQ: Recommended for production. Supports message acknowledgment and requeuing.
# Redis: Simpler setup but uses fire-and-forget. Failed messages cannot be retried.
QUEUE_TYPE=rabbitmq

# RabbitMQ Configuration (when QUEUE_TYPE=rabbitmq)
QUEUE_URL=amqp://guest:guest@goduit-queue:5672/

# Redis Configuration (when QUEUE_TYPE=redis)
# WARNING: Redis Lists don't support message requeuing. Use for development only.
# QUEUE_URL=redis://goduit-redis:6379
# For Redis with authentication:
# QUEUE_URL=redis://user:password@goduit-redis:6379/0

# Article Queue Name
ARTICLE_QUEUE_NAME=new-articles-queue

# Feed Configuration
FEED_MAX_ARTICLES=30

# JWT KEYS
JWT_PRIVATE_KEY_BASE64=
JWT_PUBLIC_KEY_BASE64=
# Logging
LOG_LEVEL=debug
